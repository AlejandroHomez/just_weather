import 'package:flutter/material.dart';

class JwSettingsScreen extends StatelessWidget {
  static const routeName = '/jw_settings_screen';
  const JwSettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Settings")));
  }
}
